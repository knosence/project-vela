# Telegram Integration

Telegram is modeled as an optional outbound operator channel for Vela.

Current state:
- config surface exists
- matrix reference exists
- governed sender logic exists
- test-send is available through CLI and service
- night-cycle delivery can send the DC report and blocked summary when enabled

Canonical matrix reference:
- `knowledge/240l.DOMAIN.Telegram-Integration-Ref.md`
